function soma(n1=0, n2=0){
    // se n1 ou n2 não tiverem valor eles assumirão o valor de 0
    return n1 + n2
}

console.log(soma( 2, 5))
    // console.log mostra na tela 
    // (soma) é o primiero parametro
    // (n1, n2) é outro parametro 